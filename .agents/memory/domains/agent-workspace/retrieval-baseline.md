# 检索改动必须跑评测闭环

## 现象与根因

改知识库正文或检索参数后，只靠 `kb-search` 抽查一两句「看起来命中」就宣布完成。改写类查询与精确标识符类查询的召回表现差异大，抽查无法暴露回归。

## 对策

任何影响检索链路的改动（正文、frontmatter、分词、融合参数）都按固定顺序收口：`kb-index --rebuild` → `kb-check` → `kb-eval`，精确标识符与改写类用例都要过。评测口径以 Stage 5 回溯后的 Parent 为准，与交付给 LLM 的内容一致。
