# 本机工具配置，随仓库提交。解释器等纯工具路径可以写入；密钥类信息与可识别个人的
# 用户主目录路径仍不得写入。路径用正斜杠（/）避免 TOML 转义。

# Python 解释器，本仓库脚本与 MCP 统一用解释器的完整路径运行；留空或缺失时
# 脚本立即失败，不回退 PATH。换机器或升级环境时只改这里。
python = "D:/ProgramData/miniforge3/python.exe"
