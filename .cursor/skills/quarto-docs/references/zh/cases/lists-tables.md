# 列表与解读案例

## 本章目标（列表三步）

引导：「学完本章，你就能：」

- 装好并启用 WSL2，让 Ubuntu 成为开发环境。
- 在 Ubuntu 里装好并验证 C++ 构建工具链。

## 工具安装表格

| 包 | 用途 |
|---|---|
| build-essential | g++、make |
| cmake | 构建系统 |

## 代码后解读

对比型知识点：先设问 + 双条结论，再给代码；多行输出用编号逐项说明。

## 详略

- 有官方文档的操作：决策 + 链接，不转抄全文。
- 验证：合并为「抽查 `g++` 与 `cmake` 的 `--version`」，不逐个工具重复段落。
