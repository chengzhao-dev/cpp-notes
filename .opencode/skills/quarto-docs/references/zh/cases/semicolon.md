# 分号示例 / 案例（writing-style.md 配套）

本文件是 `writing-style.md`「分号（；）的克制使用」的 before/after 案例库。规则见 `../writing-style.md`。

示例（WSL2 环境章「准备与安装」引言）：

```markdown
# 避免
跟着下面三步，装好 WSL2 与 Ubuntu 发行版；本节只演示最简安装方式，更多请看文档。

# 推荐
跟着下面三步，装好 WSL2 与 Ubuntu 发行版。本节只演示最简安装方式，更多请看文档。
```
