# 首页 Hero 示例 / 案例（writing-style.md 配套）

本文件是 `writing-style.md`「首页 Hero 描述（导语）写作」的格式示例库。规则见 `../writing-style.md`。

**模式一（默认 / 通用承诺）**——`帮你` + 从起点到终点的轻量收尾，语气亲切：

```markdown
覆盖开发环境、构建系统、语言核心与标准库，帮你把 C++ 从装好到写顺。
```

**模式二（具体收益）**——`帮你` + 直接点明学完能做出什么，不重列已罗列的主题：

```markdown
覆盖开发环境、构建系统、语言核心与标准库，帮你动手写出并构建自己的 C++ 程序。
```
