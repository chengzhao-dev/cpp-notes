---
kb_id: "cpp-quarto-chapter-pattern-v1"
title: "教学正文的页面块序列与职责边界"
domain: "quarto-docs"
subdomain: "page_pattern"
tags: [page, block, sequence, preface, task, recap, ordering]
level_range: [0, 9]
dependencies: ["cpp-quarto-chapter-environment-v1"]
created: "2026-09-10"
updated: "2026-09-11"
chunk_strategy: "semantic_heading"
estimated_tokens: 1500
---

# 教学正文的页面块序列与职责边界

## 块序列固定为五段的原因

读者在正文里依次进行三种不同的认知活动：确认自己来对了地方、完成一个可验证的动作、确认自己获得了什么。五段各服务一种活动，顺序由「读者先要知道什么才能看懂下一步」决定，而不是由作者手头材料的顺序决定。

```text
引言 → 概览或结构 → 任务序列 → 边界与排查 → 回顾
```

- 引言建立动机与目标，回答为什么要读这一章。
- 概览或结构给出对象的静态形状，让后面的操作有落点。
- 任务序列是主体，每段推进一个可独立验证的动作。
- 边界与排查放在成功路径之后：新手在还没跑通时读到失败分支会失去主线。
- 回顾把分散在各段的验证物收成一个结论，并给出下一入口。

打乱顺序的代价是可观察的：把验证挪到操作之前，读者不知道自己在等什么。把排查插进任务序列，同一章里成功与失败路径交替出现，读者无法判断当前处在哪个状态。

## 引言的职责边界

引言同时承担三件事：承接上一章的结果、说明本章要解决的具体问题、给出完成后可观察的验证物。超出这三件事的内容会推迟读者第一次行动的时间。

- 前置条件属于上一章的回顾或本章第一个任务的开头，写进引言会把读者引向他此刻还不需要的信息。
- 后续章节的命令与结论不出现在引言：读者无法验证，且后章一旦调整就形成事实错误。
- 引言不解释术语。术语在第一次真正使用它的任务里给出中文名称与英文检索词。
- 需要读者先看到「做成是什么样」时，给出预期输出而不是实现细节。
- 首个 C++ 程序的引言链接到完整、可直接打开的示例工程。源码细节放在独立的源码阅读任务中，不把单个文件当作章节入口。

## 一个二级标题对应一个可验证任务

任务粒度的判定标准是：任务结束时存在一个读者能自己复述的检查动作。如果结束时只是「读完了解释」，这段应该并入相邻任务。

### 新手段落的最小骨架

面向没有前置概念的读者时，每个任务优先按“目标 → 动作 → 结果”组织。目标只说明这一步要得到什么，动作只保留完成目标所需的最少操作，结果给出一个能观察或复述的成功判据。前置条件和失败分支分别放在动作之前和成功路径之后，避免一个段落同时解释背景、步骤和排错。

段落之间用因果关系连接，而不是用同义句重复中心。若一个段落出现两个以上独立动作，或读者无法指出本段结束时要检查什么，就拆成列表或相邻任务。

### 任务内部的递进

概念 → 最小示例 → 操作 → 验证 → 边界。先让读者看到行为，再解释足以理解该行为的规则。完整规则推迟到读者确实需要它的时候。先给规则后给示例时，读者会把第一次阅读用来记忆而不是理解。

### 变化点

扩展同一个示例时一次只引入一个新的语言规则或工程约束。同时改变两件事，读者无法把观察到的差异归因到其中任何一件，验证就失去反馈价值。

### 何时再分层

一个任务同时需要概念、多个操作阶段和独立验证时才切出更小的标题。只有一两句话的内容并入相邻任务。为凑层级增加的标题会让右侧目录变成噪声。

## 常见问题与本章回顾的分工

两块都在末尾，且极易被写成重复内容，分工依据是它们服务的读者状态不同。

| 块 | 读者状态 | 内容要求 |
| --- | --- | --- |
| 常见问题 | 失败了 | 按症状命名，给诊断命令和回到成功路径的动作 |
| 本章回顾 | 成功了 | 已获得的能力、确认方式、下一入口 |

回顾写成读者能自述的结果，不复述小节标题：复述目录会让读者误以为读完列表等于完成任务。常见问题不解释原理，原理属于它所在的任务段。这里只回答「看到这个现象，该做什么」。

## 元素选择的判定依据

元素由信息之间的关系决定，视觉偏好不构成理由。

| 信息关系 | 元素 |
| --- | --- |
| 项目之间无先后依赖 | 无序列表 |
| 调换顺序会导致失败 | 有序列表 |
| 三项以上且需横向比较属性 | 表格 |
| 需保留空格层级或逐字呈现输出 | 纯文本代码块 |
| 读者要复制执行 | 标明语言的代码块 |
| 会踩坑但不属于主线的一句话 | 内置提示框 |
| 需要逐字呈现的真实文件 | 引用仓库源码 |

提示框承载规则、风险和易忘项，不承载必经步骤：步骤被放进提示框后，扫读的读者会直接漏掉它。表格用于比较，不用于罗列长句。每格放得下一句完整话时就改用列表。

同一类信息该放哪个元素之外，还有「密度」这一维：一页里盒子过多会把正文挤成图注。阈值与成因见标识 `cpp-quarto-typography-density-v1` 的知识文件。

## 块序列跨多个页面时如何连接

块序列描述单页内部的顺序，不能整条套到章节集合上：教程集合里排第一的页面承担的是入口职责而不是概述职责，它的页面形状由标识 `cpp-quarto-landing-pattern-v1` 的知识文件决定。

判断依据是读者当前要做的动作。需要他先做选择时用入口与卡片页，他已经确定要完成某个任务时用教学正文。同一份内容出现在两类页面上时，只有正文那一处承担可验证任务，入口那一处退化成定位与下一步。

## 与其他知识文件的分界

本文件只回答页面由哪些块组成、每块的职责边界和顺序依据。段落、句式与措辞案例见标识 `cpp-quarto-chinese-style-v1` 的知识文件。环境、命令与工程展示案例见标识 `cpp-quarto-chapter-environment-v1` 的知识文件。渲染与输出取值见标识 `cpp-tooling-quarto-render-v2` 与 `cpp-tooling-quarto-html-v2` 的知识文件。入口与卡片页的组织见标识 `cpp-quarto-landing-pattern-v1` 的知识文件。
