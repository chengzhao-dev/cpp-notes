---
title: "搭建 WSL2 开发环境"
---

这份笔记统一使用 Ubuntu 作为 Linux 环境。完成本章后，后续命令都在 Ubuntu 中执行。WSL2 让 Windows 可以运行 Linux 环境，本章只完成一件事：准备可以编译、运行和调试 C++ 程序的环境。

## 准备 Linux 开发环境

### 安装 WSL2 和 Ubuntu

Linux 有多个发行版，这份笔记统一使用 Ubuntu。发行版可以理解为围绕 Linux 内核组合系统工具、软件包和管理方式的一套完整环境。

以管理员身份打开 Windows PowerShell，执行下面的命令，安装 WSL2 和默认的 Ubuntu 发行版。

```{.powershell filename="PowerShell（管理员）"}
# 安装 WSL2 和默认的 Ubuntu 发行版
wsl --install
```

命令执行完成后，按提示重启 Windows，让新启用的 WSL2 组件生效。重启后首次启动 Ubuntu，按提示创建 Linux 用户名和密码。输入密码时不会显示字符，这是正常现象。

创建账号后，终端会显示提示符。请对照下面的格式，确认是否已经进入 Ubuntu，并了解后续命令的执行位置。

::: {.callout-note title="提示符与终端范围"}
看到类似 `user@host:~$` 的提示符，就说明已经进入 Ubuntu。后续 `bash` 命令都在这个终端中执行。

- `user` 是用户名。
- `host` 是电脑名称。
- `~` 是当前用户的主目录。
:::

### 安装 C++ 工具链

进入 Ubuntu 终端后执行下面的命令。先更新软件包列表，再安装编译器、构建工具、调试器和语言服务。

```{.bash filename="Ubuntu Bash"}
# 更新软件包索引
sudo apt update

# 升级已安装的软件包
sudo apt upgrade -y

# 安装 Clang/LLVM 编译、构建、调试和语言服务工具
sudo apt install -y clang clangd llvm lldb libc++-dev libc++abi-dev cmake ninja-build
```

::: {.callout-tip title="下载较慢时再更换软件源"}
更换软件源只改变软件从哪里下载，不会改变 Ubuntu 版本。下载较慢时，按[清华大学开源软件镜像站的 Ubuntu 软件源帮助](https://mirrors.tuna.tsinghua.edu.cn/help/ubuntu/)完成配置，再重新执行本节命令。
:::

## 常见错误

### 虚拟机平台未启用

执行 `wsl --install` 后提示“虚拟机平台未启用”，安装无法继续。

1. **定位缺失项**

   在电脑启动设置中确认 CPU 虚拟化已经开启。再检查 Windows 功能列表中的“虚拟机平台”和“适用于 Linux 的 Windows 子系统”是否已经启用。

2. **启用缺失项并重启**

   在“启用或关闭 Windows 功能”中启用缺少的项目。如果电脑启动设置中尚未开启 CPU 虚拟化，也先将其开启，再重启 Windows。

3. **重新安装并验证**

   以管理员身份再次执行 `wsl --install`。首次启动 Ubuntu 后出现 Linux 提示符，就回到[安装 WSL2 和 Ubuntu](#安装-wsl2-和-ubuntu) 继续完成账号创建。

### 找不到命令

在 Ubuntu 中运行某个 C++ 工具时提示 `command not found`。

1. **定位缺失工具**

   执行下面的命令，查看哪些工具没有输出路径。

   ```{.bash filename="Ubuntu Bash"}
   # 检查工具是否已经安装
   command -v clang++ clangd lldb cmake ninja
   ```

2. **安装缺失工具**

   执行下面的命令，刷新软件包索引并安装完整的 C++ 工具链：

   ```{.bash filename="Ubuntu Bash"}
   # 刷新软件包索引
   sudo apt update

   # 安装 C++ 编译、构建、调试和语言服务工具
   sudo apt install -y clang clangd llvm lldb libc++-dev libc++abi-dev cmake ninja-build
   ```

3. **验证安装**

   再次执行定位命令，确认每个工具都输出路径。

   ```{.bash filename="Ubuntu Bash"}
   # 重新检查工具是否已经安装
   command -v clang++ clangd lldb cmake ninja
   ```

## 本章回顾

在 Ubuntu 终端执行 `command -v clang++`。如果输出路径，说明编译、构建和调试工具已经准备好。下一章会创建第一个源文件，并直接编译运行它。
